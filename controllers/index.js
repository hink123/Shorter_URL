module.exports = {
    shortenUrl,
    lengthenUrl
}

function shortenUrl(req, res) {
    try {

    } catch (err) {
        console.log('Oops something went wrong!')
        res.json({err});
    }
}

function lengthenUrl(req, res) {
    try {

    } catch (err) {
        console.log('Oops something went wrong!')
        res.json({err});
    }
}